name: 'build-bareos-app'
description: 'Build Bareos app images'
author: 'dark-vex'
inputs:
  registry:
   description: 'Registry URL'
   required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.registry }}
