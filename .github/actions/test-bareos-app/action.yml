name: 'test-bareos-app'
description: 'Test Bareos docker images'
author: 'dark-vex'
inputs:
  registry:
   description: 'Registry URL'
   required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.registry }}
